apply plugin: 'findbugs'

tasks.withType(FindBugs) {
    reports {
        xml.enabled true
        html.enabled true
    }
}

findbugs {
    effort = "max"
    reportLevel = "high"
    ignoreFailures = false
}