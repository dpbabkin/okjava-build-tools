apply plugin: 'checkstyle'

def getCheckStyleConfigFile() {
    def url = project.buildscript.classLoader.getResource('checkstyle/checkstyle.xml').toURI();
    return new File(url)
}

checkstyle {
    toolVersion = "6.19"
    ignoreFailures = false
    configFile = getCheckStyleConfigFile()
}
