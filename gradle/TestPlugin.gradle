apply plugin: TestPlugin

class TestPlugin implements Plugin<Project> {
    void apply(Project project) {
        println ' I m in sige TestPlugin'

        project.task('hello_TEST') << {
            println 'hello_TEST '
        }
    }
}